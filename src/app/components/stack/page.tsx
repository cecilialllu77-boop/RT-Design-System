import { ComponentPage } from "@/components/component-page";

export default function StackPage() {
  return (
    <ComponentPage
      title="Stack"
      description="Layout children with consistent spacing in a row or column."
    />
  );
}
