import { ComponentPage } from "@/components/component-page";

export default function ModalPage() {
  return (
    <ComponentPage
      title="Modal"
      description="A dialog overlay that requires user interaction."
    />
  );
}
