import { ComponentPage } from "@/components/component-page";

export default function LoadingPage() {
  return (
    <ComponentPage
      title="Loading"
      description="Indicate that content is being loaded."
    />
  );
}
