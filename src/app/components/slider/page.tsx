import { ComponentPage } from "@/components/component-page";

export default function SliderPage() {
  return (
    <ComponentPage
      title="Slider"
      description="Select a value from a range using a draggable handle."
    />
  );
}
