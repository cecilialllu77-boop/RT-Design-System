import { ComponentPage } from "@/components/component-page";

export default function TextPage() {
  return (
    <ComponentPage
      title="Text"
      description="Render text with consistent styling and semantic markup."
    />
  );
}
